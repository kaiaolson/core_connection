<hr>
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#profile-navbar" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Profile Wizard</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <!-- <div class="collapse navbar-collapse" id="profile-navbar">
      <ul class="nav navbar-nav">
        <% if @profile.id %>
        <li><%= link_to "Basic Information", edit_profile_path(@profile) %></li>
        <li><%= link_to "Projects", profile_projects_path(@profile) %></li>
        <li><%= link_to "Education", profile_educations_path(@profile) %></li>
        <li><%= link_to "Experience", profile_experiences_path(@profile) %></li>
        <li><%= link_to "Skills", profile_skills_path(@profile) %></li>
        <% else %>
        <li><%= link_to "Basic Information", new_profile_path(@profile) %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse --> -->
  </div><!-- /.container-fluid -->
</nav>
