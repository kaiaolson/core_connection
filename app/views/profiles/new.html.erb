<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#profile-navbar" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Profile Wizard</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="profile-navbar">
      <ul class="nav navbar-nav">
        <li><%= link_to "Basic Information", new_profile_path(@profile) %></li>
        <li><%= link_to "Links", new_profile_link_path(@profile) %></li>
        <li><%= link_to "Projects", new_profile_project_path(@profile) %></li>
        <li><%= link_to "Education", new_profile_education_path(@profile) %></li>
        <li><%= link_to "Experience", new_profile_experience_path(@profile) %></li>
        <li><%= link_to "Skills", new_profile_skill_path(@profile) %></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
