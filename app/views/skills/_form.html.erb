<%= simple_form_for @profile do |e| %>
  <%= e.simple_fields_for :skill do |f| %>
    <%= f.input :name %>
    <%= f.input :category_id, collection: Category.order("name"), default: 1 %>
    <%= f.submit class: "btn btn-primary" %>
  <% end %>
<% end %>
