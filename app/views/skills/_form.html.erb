<%= simple_form_for [current_user_profile, @skill] do |f| %>
  <%= f.input :name %>
  <%= f.input :category_id, collection: Category.order("name"), default: 1 %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
